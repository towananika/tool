# 間合い — 変更履歴

## v1.6 (2026-09-06)
- **道具の検索を追加** — ホーム上部の一行に入力すると絞り込まれる。かな・漢字・ローマ字・韓国語・英語のいずれでも当たる（「ちょんせ」「전세」「gas」「資産」など）。Enterで先頭の道具が開く
- **「よく使う」を自動で先頭に表示** — 開いた回数を記録し、上位3つをホームの一番上に出す。合計4回使うまでは表示しないので、最初から散らからない
- 道具が増えても到達までの手数が変わらないようにするための変更

## v1.5 (2026-09-06)
- **引用されるための構造を整備**
  - 計算式と前提を静的HTMLで公開（クローラーやAIが読める本文。従来はJavaScript生成のみで中身が空だった）
  - JSON-LD構造化データ（WebApplication / FAQPage）、OGP、canonical、robots.txt、sitemap.xml を追加
  - MITライセンスを明記し、出典表記の例をページ内に記載
  - **道具ごとの直リンクに対応**（例: `#/house`, `#/jeonse`）。特定の計算機を名指しで共有できる
- **自然の配色を適用** — 空＝はかる、海＝くらべる、木の緑＝お金と住まい。アイコンと見出しの点にだけ色を使い、背景は生成りのまま
- 名前の主従を入れ替え、日本語名「間合い」を主に

## v1.4 (2026-09-06)
- **「チョンセ融資」タブを追加** — 청년 전세대출と一般 전세대출の差額を計算
  - 限度額を超えた分は一般金利で借りる前提で自動的に混合計算
  - 所得控除（元利返済額の控除率・年間上限・適用税率）による還付を差し引いた実質負担を表示
  - 월세との比較も併記。自己資金と保証金の機会費用を含めて年額で比べる
  - 金利や限度額はすべて入力式（制度改定に追従できるようにするため）

## v1.3 (2026-09-06)
- **タブをやめて、ホーム画面（メニュー）方式に変更** — 11個のタブが4段に折り返して押しにくくなっていたため
  - 大きなタイルを「はかる／くらべる／お金と住まい／その他」の4群に整理
  - 道具を開くと上部に戻るボタンと現在地の名前が出る。ホームでは上部バーごと畳んで余白を作らない

## v1.2 (2026-09-06)
- **「住居の重み」タブを追加** — 毎月の固定費を並べ、それぞれを同じ割合だけ削ったときの将来差額を比較する
  - 住居費が手取りの30%を超えると警告
  - 「住居を10%削るほうが、サブスクを丸ごとゼロにするより効く」といった結論を自動で文章化
  - 節約の効果は運用利回りを含めた将来価値で計算するため、年数を伸ばすほど差が開くことが見える

## v1.1 (2026-09-06)
- **設定タブを追加。データの書き出し／読み込みに対応** — 別の端末へ移すには、書き出したファイルか文字列を相手の端末で読み込むだけ。データは端末の外に出ないまま移せる
- 保存されているデータの一覧と、全消去も設定から可能に
- **デスクトップ表示を改善** — 880px以上では横幅860pxに広がり、入力欄が2列に並ぶ

## v1.0 (2026-09-06)
- **タブにアイコン＋名前を表示**（アイコンだけでは意味が伝わらないため）。3群に整理
- **株 vs 不動産を追加** — 同じ自己資金を株に入れる場合と、レバレッジを効かせて不動産を買う場合で比較。税・空室・保有費・売却費用を考慮し、不動産が株に並ぶ価格上昇率を二分探索で算出
- **給油くらべを燃料4種に対応** — ガソリン／軽油／LPG／電気。電気はkm/kWh・kWh表記に自動で切り替わり、種別ごとに設定を保存
- **為替を3層表示に刷新** — 基準レート／銀行の現金レート／両替所レート（手入力）。主役は受取額ひとつだけ表示し、「銀行より+2.87%／基準より+1.06%」の1行で判断できる形に。詳細は折りたたみ
- **為替レートの自動更新を追加**（GitHub Actions、平日 09/12/15/18 KST）。Dunamuの公開APIから매매기준율・현찰 살 때・현찰 팔 때を取得し rates.json に保存
- 両替所は場所も重要なため、マイバンクの地図検索へのリンクを設置

## v0.9 (2026-09-06)
- **資産テーブルを追加** — 現金／投資／拘束／負債の4区分で棚卸しし、純資産と現金比率を自動計算。現金比率が10%を切ると警告
- **家賃 vs 保有を追加** — 월세・전세・매매を同じ手元資金から出発させて毎月シミュレーションし、N年後の純資産で比較
  - 住宅価格上昇率を二分探索し、「매매が賃貸に並ぶ分岐点は年◯%」を自動算出
  - 전세대출は利息のみ、住宅ローンは元利均等で計算。取得税・保有コスト・管理費・投資機会費用をすべて考慮
- 資産データは端末のlocalStorageのみに保存（リポジトリにも外部にも送信されない）

## v0.8 (2026-09-06)
- **ストップウォッチを追加**（ラップ計測、最速/最遅の色分け表示）
- **給油所くらべを追加** — 往復で余分に燃やす燃料代を上乗せして、遠くの安い店まで走る価値があるかを判定
- **現在価値を追加** — 将来の入金予定を記録として積み重ね、いまの価値に割り引いて合計する台帳型
- **為替を追加** — 銀行レート（ECB基準）を自動取得し、手入力した両替所レートと差額・％で比較。通貨ペアごとに保存
- タブを「基本ツール（タイマー・ストップウォッチ・カウンター）」と「計算ツール（給油所・現在価値・為替）」の2群に整理
- カウンター音を刷新：金属の共振と筐体の胴鳴りを加え、実物のタリーカウンターに近い打撃音に
- 10単位ごとの音を明確に強調（長さ9倍・ピーク音量3倍の3段構成「ガチャン」）
- 見出しに英字表記「Pace」を併記（日本語名は「間合い」のまま）

## v0.7 (2026-09-01)
- **アンビエントサウンドを追加**（設定でON、デフォルトOFF、音量調整可）
  - 研究知見に基づき、集中中はブラウンノイズ（集中力・ワーキングメモリ向上）、休憩中はピンクノイズ（リラックス・コルチゾール低下）を自動でクロスフェード再生
  - 外部音源不要、その場で合成
- スクリーンセーバーの太陽/月マーカーを廃止し、**山並みのシルエット風景**に変更。日照の明るさに応じて画面全体に明暗のオーバーレイをかける
- **日照モードON時、カード全体にも暖色フィルター**をかけるように（f.lux/Night Shiftと同じ原理でブルーライトを抑える、概日リズムに配慮した配色）

## v0.6 (2026-09-01)
- **日照モードを追加**（設定でON、デフォルトOFF）
  - 位置情報→タイムゾーン推定→PC時刻オフセット推定の3段階フォールバック、緯度経度は手動入力も可
  - 実際の日の出・日の入り時刻をもとに、背景が朝焼け／昼／夕焼け／夜と滑らかに変化
  - 「全体表示（スクリーンセーバー）」モード：画面いっぱいの空＋太陽/月マーカー＋大きな時計。タップで復帰
- カウンター音を再調整：通常音は機械的な「カチッ」、10単位ごとはリセットレバーの重い一撃音「カッチ」
- 再生／一時停止アイコンを角丸のSVGに刷新（アプリ全体の丸みのある言語と統一）
- 不具合修正：スクリーンセーバーの`hidden`属性がCSSに上書きされ常時表示されていた問題を修正

## v0.5 (2026-09-01)
- **不具合修正**: つまみ(ノブ)の位置がリングの弧の終点とズレていた計算ミスを修正
- **不具合修正**: 一時停止すると経過時間が消えて満タン表示に戻ってしまう不具合を修正。再開も一時停止した時点から続くように
- **不具合修正**: モバイルでビューポート設定が無く、画面全体が縮小されて見えていた問題を修正
- **反応速度改善**: タイマーのリング・カウンターの丸ボタンをpointerイベント直接反応に変更し、体感の遅延を解消
- ベルの音を集中→休憩／休憩→集中で区別（4種類とも音程・リズムが変化）
- 4サイクルごとに自動で長め休憩（通常の3倍）
- 今日の集中回数・合計時間を記録（日付が変わると自動リセット）
- ホーム画面に追加できるPWA対応（manifest.json・アイコン・Service Worker）
- カウンターに機械式カウンター風の「かちかち」音、10単位ごとに「しゃかっ」という違う音
- カウンター加算時にランダムなアイコンが浮かんで消える演出、対応端末では振動フィードバック
- リセットボタンに確認ポップアップ（進行中の時だけ確認、何もなければスルー）
- タップ時の青いハイライト表示を除去

## v0.4 (2026-09-01)
- **不具合修正**: タイマーとカウンターの両画面が同時に表示されてしまうバグを修正（`hidden`属性がCSSの`display:flex`に上書きされていた）

## v0.3 (2026-09-01)
- **カウンター機能を追加**: タイマーと切り替えられる新しいタブ
  - 円をタップで+1、キーボード操作対応（↑/Enter/Space=+1、↓/Backspace=−1）
  - 増減幅を設定可能
  - 値はブラウザに保存され、閉じても消えない

## v0.2 (2026-09-01)
- **操作方法を物理タイマー風に刷新**: 数字入力・ボタンから、リングをドラッグして時間を設定する方式へ
  - リング中央をタップで開始/一時停止（アイコンのみ、文字なし）
  - 設定（プリセット・休憩時間・ベル音）は歯車アイコンの奥に格納
- **不具合修正**: タブがバックグラウンドで間引かれても、正しい残り時間に復帰するよう内部の計測方式をタイムスタンプ基準に変更
- **不具合修正**: ダイヤルで時間を設定してもカウントダウンが反映されない不具合を修正

## v0.1 (2026-08-31)
- 初版公開
- 集中時間・休憩時間を設定できるタイマー
- ベル音を4種類から選択（鐘・チャイム・ビープ・木魚）、すべてWeb Audioでその場合成
- 集中↔休憩の自動切り替え
- 休憩中の行動案内メッセージ（画面から離れる・伸びをする等）をランダム表示
- プリセット（深い集中90/20・浅い集中25/5・52/17）
- MUJI×韓屋の方向性でデザイン（生成り色・木の色・控えめな線）
