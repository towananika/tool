# 間合い — 変更履歴

## v3.2 (2026-09-10)
- **訪問者を数える口を用意した** — フッターに「のべ◯人が使いました」を出す。**GitHub Pages は静的配信なので、このページから訪問者のIPは見えない**。「同じIPは1日1回」を満たすには外の集計に任せるしかないため、GoatCounter を使う形にした（IP＋ブラウザ＋日替わりの塩をハッシュして数えるので、**クッキーを置かずに「同じ人はその日1回」**になり、IPそのものは保存されない）
- **未設定のあいだは何も起きない** — `COUNTER_SITE` が空なら外部スクリプトを読み込まず、表示も出さない。アカウントを作って名前を入れた時点で動き出す

## v3.1 (2026-09-10)
- **現在価値に外貨を入れた** — 記録ごとに通貨を選べる。外貨は**その通貨の金利で割り引いてから、今のレートでウォンに直す**。行ごとに先物レートも出す
- **先物レートは予想ではない** — 今のレート × ((1+ウォン金利)÷(1+外貨金利))^年数。カバー付き金利平価そのもので、「今その通貨を借りて／貸して固定するならこの値でしか成立しない」という理論値。**金利の高い通貨ほど先物では安くなる**（高金利は将来の値下がりとセットで価格に入っている）
- レートは為替と同じ `rates.json` を共有。外貨の記録があるときだけ取りに行く
- 計算式と限界は「計算式と前提」ページに追記

## v3.0 (2026-09-09)
- **道具を追加: 「いつ届く」** — 目標の金額・いまの資産・毎月の積立・年利を入れると、何年で届くかと、その内訳（自分で入れる分／利回りが作る分）を出す
- **本体は年数ではなく「どれを動かすと早いか」** — 積立+50万／積立1.5倍／年利+1%／目標を2割下げる、の4つを同じ土俵で並べ、それぞれ何年縮むかを出す。**利回りを追うより積立を増やすほうが効く場面が多い**ことが、その場で分かる
- 入力は端末に保存（`pace-goal`）。計算式と限界は「計算式と前提」ページにも載せた

## v2.9 (2026-09-09)
- **カウンターの音を2種類から選べるようにした** — 設定に「木／カチカチ」。選んだ瞬間に一度鳴る（押して確かめられないと選べない）。選択は端末に保存する
- **カチカチ（金属の数取器）** — 木で消した要素を、こちらでは全部立てている。共振を高く短く（1600〜5200Hz・30ms以下）、空気の抜けを 3000Hz/0.16、励起の立ち上がりを0.6msに尖らせ、二打目を **8ms後・0.38**（これが「カチカチ」の正体）
- **10単位も音色ごとに変えた** — 木は一段低く長く残す音、カチカチは**桁車が0まで戻る連射**（v2.7まで使っていた形）。金属の数取器はこれが実物の音なので、こちらでは正しい

## v2.8 (2026-09-09)
- **PCとモバイルで音量を分けた** — PCは机の上、モバイルは手の中で使う。距離も内蔵スピーカーも違うので、**PCは3割下げる**。タッチ操作の端末かどうか（`pointer: coarse`）で判定する。圧縮の手前を下げても頭打ちで効かないので、出口で下げている
- **音色を「効果音」から離した** — 明るく尖った成分を落とした。空気の抜けを 1500Hz/0.22 → **700Hz/0.05**（ここが立つと「ピッ」という電子音になる）、励起の立ち上がりを 1ms → **4ms のなだらかな山**（角が立つと高い成分が散る）、共振体を全体に少し低く（430→392Hzほか）、1打の二つ目を 9ms/0.30 → **18ms/0.12**（近くて強いと「カチカチ」という機械の音になる）
- **10単位を作り直した** — 桁車が0まで戻る連射（打撃30発ほど）をやめ、**同じ木を一段低く叩いて長く残す**音にした。機構としては連射のほうが正しいが、何百回も聞く道具ではゲームの効果音に聞こえる。区切りとしての分かりやすさは、オクターブ下が3倍長く残ることで付けている
- **実測** — 歪み(3k超) 0.105→**0.057（−46%）**／1打の実効値 0.166→0.172（**音量は維持**）／1打ピーク 0.80／クリップ0／10単位は1打の1.24倍の実効値

## v2.7 (2026-09-09)
- **メニューの「よく使う」をやめた** — 上位3つが先頭に出ると、道具の並びが日によって変わる。**同じ道具がいつも同じ場所にある**ほうが速いので、分類（時間／お金／住まい）だけにした
- 並べ替えのために貯めていた使用回数（`pace-usage`）も記録をやめ、既存のぶんは開いたときに捨てる

## v2.6 (2026-09-09)
- **音は大きいまま、歪みを減らした** — v2.5は飽和(tanh40)で音量を稼いでいたので、木ではなく「潰れた」音になっていた。3kHz超の倍音を測る指標を計測ツールに足して、歪みの量を数字で見られるようにした
- **やり方を変えた** — 飽和で倍音を*作る*のをやめ(tanh40→**tanh8**)、代わりに**共振体を3つ→5つに増やして長く鳴らす**(430/620/880/1180/1620Hz)。実物の木の胴にも共振はもっとある。元からある共振を足すほうが、大きくしても濁らない。足りない密度は圧縮で補う(しきい −9→−14dB、比 20→12、出口 1.18→1.35)
- **実測** — 歪み(3k超) 0.123→**0.092（−25%）**／1打の実効値 0.185→0.171（**音量は93%を維持**）／1打ピーク 0.87／10単位ピーク 0.95・**クリップ0**
- 10単位は全体を 0.30→**0.45** に戻した。飽和が減って頭が詰まらなくなったぶん、上げられる

## v2.5 (2026-09-09)
- **カウンターの音をもう一段大きくした** — 入力 90→250、飽和 tanh20→tanh40、安全弁のしきい −6→−9dB、出口 1.10→1.18、10単位の全体 0.42→0.30
- **実測** — 1打の実効値 0.136→**0.178（1.31倍。v2.2からは6.9倍）**／1打ピーク 0.68→0.85／10単位ピーク 0.95・**クリップ0**
- **10単位と1打の差が縮んだ**（比 1.40→1.12）。10単位のピークは安全弁で0.95に張り付いていて上げられないので、1打を上げるぶんだけ差が詰まる。実効値では倍あるので区切りは聞き分けられるが、v2.4より控えめになった
- **ここが実質の上限**。これ以上（入力400以上）は1打のピークが1.0に触れて割れる

## v2.4 (2026-09-09)
- **カウンターの音をさらに大きくした** — v2.3で密度は3.1倍になったが、まだ小さいという判断。同じ計測ツールで7案を測り、割れない範囲でいちばん大きい設定を選んだ
- **中身** — 入力 30→90、飽和 tanh6→tanh20、安全弁のしきい −3dB→−6dB、出口 1.00→1.10、胴の鳴り 130ms→200ms（Qも8→10）。10単位は重なりが増えるぶん全体を 0.55→0.42 に下げて、天井に当たらないようにした
- **実測** — 1打の実効値 0.075→**0.131（1.75倍。v2.2からは5.2倍）**／1打ピーク 0.56→0.67／10単位ピーク **0.95・クリップ0**／10単位と1打の比 1.43（区切りは引き続きはっきり大きい）
- 飽和を強くしたので、v2.3より少し歪んだ・詰まった音になる。これは音量と引き換えで承知の上

## v2.3 (2026-09-09)
- **カウンターの音が小さい問題を、測ってから直した** — これまで3回、音量を上げる方向で作り直して失敗していた。実測すると原因は音量ではなかった。ピークは0.60まで出ているのに実効値は0.026しかない。打撃音は一瞬で消えるので、**ピークが天井に当たっていても耳には小さい**。上げれば割れるだけで、大きくは聞こえない
- **直し方を3つ同時に入れた** — ①飽和（tanh）を挟む。木は非線形に鳴るので、潰すと倍音が増え、ピークを上げずに密度が出る ②圧縮を「常にかける機械」から「振り切れる時だけ止める安全弁」に変える（しきい −20dB・比10 → −3dB・比20） ③胴の鳴りを52ms→130msに伸ばす。短すぎてエネルギーが乗っていなかった
- **実測の結果** — 実効値 0.026→**0.080（3.1倍）**／ピーク 0.60→0.57（**むしろ下がる**）／打ごとのばらつき 0.50〜0.67→**0.56〜0.58**／10単位のクリップ **0**／10単位と1打の比 1.01→**1.64**（区切りがはっきり大きくなった）
- 副産物として、打つたびに音量が揺れる癖も消えた

## v2.2 (2026-09-08)
- **カウンターの音を木魚にした** — 数取器はもともと念仏や読経の回数を数える道具で、木魚も同じ場で使う。元から同じ用途の道具どうしなので、音を借りても後付けにならない。木は高域が乏しいぶん、鋭い音による驚愕反射を引きにくい（集中のための道具として理にかなう）
- **音を機構から組み直した** — 桁車が1コマ回るときの一撃を部品にして、1打はそれを2回（爪が歯を乗り越える音と、そのあと座る音）、10単位とリセットは4つの桁車が0まで戻る連射として作る。同じ機構から出る音なので部品を共有している
- **リセット操作でも同じ音が鳴るようにした** — 実機と同じ挙動
- **アラーム画面が横にはみ出していたのを直した** — 時刻入力が縮まず、行がカードごと画面外へ広がっていた（実測394px / 画面375px）。端末の言語によって「午前」の表記が付き横幅が伸びるため起きる
- ホームのタイル説明で、最後の1字だけが次行に落ちるのを直した

## v2.1.1 (2026-09-08)
- **全体モードで右端にスクロールバーが出ていたのを直した** — 画面を覆っているだけで背後のページはスクロールできる状態のままだった。タイマー・カウンターのどちらの全体モードでも、開いている間は背後を止める

## v2.1 (2026-09-08)
- **アラームを追加** — 決めた時刻にベルが鳴る。繰り返しは毎日／平日／週末／一度だけから選ぶ。「一度だけ」は鳴った後に自分で止まる。鳴っている間は「5分後」で先送りできる。各行に次に鳴るまでの残り時間を出し、時刻順に並べる。ベルの種類はタイマーと共通。**ブラウザがページを開いている間だけ動く**（閉じると鳴らない）
- **タイマーの全体モードを作り直した** — 画面下を占めていた茶色い丘のシルエットを削除。数字は太字から細字にし、影も外した。光を遮るものが無いほうが時間が澄んで見えるため。背景は上に光を薄くのせたグラデーションにし、昼は白へ抜き、夜は青を差す。作動中は残り時間を細い線で示す（数字を読まなくても進み具合が分かる）
- **カウンターのリセットを取り消せるようにした** — 確認ダイアログをやめ、押した瞬間に0に戻して9秒間「元に戻す」を出す。確認で止めると数える流れが切れるうえ、「はい」を押す癖がつくと結局は守りにならないため。0のときはリセットが薄く表示される
- **意見の受け口に総当たり防御を追加** — 読み出しは同一IPから1時間に10回まで。合言葉は最後まで比べてから判定し（一致した文字数が応答時間に出ないように）、失敗時は0.7秒待たせる

## v2.0.1 (2026-09-07)
- **意見の受け口を稼働させた** — Cloudflare Workers + KV に接続。書き込み口だけを公開し、読み出しは合言葉が要る。連投は同一IPから1時間5件まで（IPそのものは保存せず、ハッシュだけを鍵に使う）
- **プライバシー表記を実態に合わせた** — 意見だけは端末の外に出るため、「データは端末内にのみ保存」を「計算データは端末内にのみ保存し、外に出るのは自分で書いて送った意見だけ」に改めた

## v2.0 (2026-09-07)
- **ホームの分類を4群から3群へ** — 「はかる／くらべる」（動詞）と「お金と住まい」（対象）で分類の軸が混ざっていた。そのため家賃vs保有や株vs不動産のように、文字どおり"くらべる"道具が別の群に散っていた。軸を対象に揃え、**時間／お金／住まい**の3群にした
- **設定をタイルから外した** — 設定は道具ではなくアプリ自体の機能なので、道具の棚に並べない。ホーム上部の探す行の端に小さな入口を置いた
- **隠れていたタブバーを撤去** — v1.3でタブ方式をやめた際に `display: none` で隠しただけで残っており、しかも道具の名前をその隠れたボタンから読んでいた。名前は定数に移し、markupとCSSを削除した
- **意見を送る窓を設定に追加** — 使って困ったこと・ほしい道具・間違いの指摘を書いて送れる。返信の経路は持たない一方向。送信は「送る」を押したときだけで、自動で外に出るものはない。送り先が未設定のあいだは端末内に溜め、用意でき次第まとめて送る
- **カウンターの全体モードにリセットを追加** — 確認を挟んで0に戻す。リセット後も全体モードは開いたまま

## v1.9 (2026-09-07)
- **カウンターに全体モードを追加** — 画面ぜんぶがカウント面になる。押す場所を狙わなくていいので、手元を見ずに数え続けられる（モバイルで数取器として使うための表示）。数えている間は画面が消えない
- **桁送りの音が10単位ごとに鳴るようになった** — これまでは「カウントが10の倍数ちょうど」のときだけ鳴る判定だったため、増減幅が3や7だと10の倍数を踏まず、一度も鳴らなかった。10の区切りをまたいだかで判定するよう変更
- **タイマーの全体表示を「全体モード」に変更** — 位置情報を入れていなくても開けるようになった（従来はボタンが無反応だった）。位置がない場合は端末の時刻から日の出・日の入りを推定して背景の明るさを変える
- **全体モードでタイマーの残り時間を表示** — 作動中は残り時間と「集中／休憩」、停止中は時計。表示中は画面が消えない

## v1.8 (2026-09-06)
- **ホームから説明文を外した** — 計算式・前提・限界・引用のまとめは、これまで全画面の下に出続けていた。独立した静的ページ `about.html`（「計算式と前提」）に移し、ホームは道具のタイルだけになった
- **たどり着き方は4つ** — 設定 →「計算式と前提を見る」／ 検索（「計算式」「前提」「引用」「使い方」「about」「설명」など。ホームのタイルには並ばない検索専用の項目）／ ページ下部の小さなリンク ／ 旧 `#/about` も転送される
- **引用のされやすさは落とさない** — 静的HTMLのまま独立させたので、クローラーとAIからは今まで通り（むしろ単独URLになった分、名指しで引用しやすい）読める。sitemap.xml にも追加
- 「よく使う」には出さない（説明ページが道具を押しのけないため）

## v1.7 (2026-09-06)
- **条件の共有リンクを追加** — 家賃vs保有・株vs不動産・チョンセ融資・住居の重み・給油くらべ・現在価値の6つで、入力した条件をURLに載せて渡せる。受け取った側が開くと同じ数字で再現される
- **検索しなくても分かるホームに** — タイルを2列にし、名前の下に一行の説明を追加（例: 現在価値「将来のお金を今の価値に」）。検索は近道であって、必須ではない設計に
- 住居の重みのアイコンを天秤に変更（家型のアイコンが重複していたため）

## v1.6 (2026-09-06)
- **道具の検索を追加** — ホーム上部の一行に入力すると絞り込まれる。かな・漢字・ローマ字・韓国語・英語のいずれでも当たる（「ちょんせ」「전세」「gas」「資産」など）。Enterで先頭の道具が開く
- **「よく使う」を自動で先頭に表示** — 開いた回数を記録し、上位3つをホームの一番上に出す。合計4回使うまでは表示しないので、最初から散らからない
- 道具が増えても到達までの手数が変わらないようにするための変更

## v1.5 (2026-09-06)
- **引用されるための構造を整備**
  - 計算式と前提を静的HTMLで公開（クローラーやAIが読める本文。従来はJavaScript生成のみで中身が空だった）
  - JSON-LD構造化データ（WebApplication / FAQPage）、OGP、canonical、robots.txt、sitemap.xml を追加
  - MITライセンスを明記し、出典表記の例をページ内に記載
  - **道具ごとの直リンクに対応**（例: `#/house`, `#/jeonse`）。特定の計算機を名指しで共有できる
- **自然の配色を適用** — 空＝はかる、海＝くらべる、木の緑＝お金と住まい。アイコンと見出しの点にだけ色を使い、背景は生成りのまま
- 名前の主従を入れ替え、日本語名「間合い」を主に

## v1.4 (2026-09-06)
- **「チョンセ融資」タブを追加** — 청년 전세대출と一般 전세대출の差額を計算
  - 限度額を超えた分は一般金利で借りる前提で自動的に混合計算
  - 所得控除（元利返済額の控除率・年間上限・適用税率）による還付を差し引いた実質負担を表示
  - 월세との比較も併記。自己資金と保証金の機会費用を含めて年額で比べる
  - 金利や限度額はすべて入力式（制度改定に追従できるようにするため）

## v1.3 (2026-09-06)
- **タブをやめて、ホーム画面（メニュー）方式に変更** — 11個のタブが4段に折り返して押しにくくなっていたため
  - 大きなタイルを「はかる／くらべる／お金と住まい／その他」の4群に整理
  - 道具を開くと上部に戻るボタンと現在地の名前が出る。ホームでは上部バーごと畳んで余白を作らない

## v1.2 (2026-09-06)
- **「住居の重み」タブを追加** — 毎月の固定費を並べ、それぞれを同じ割合だけ削ったときの将来差額を比較する
  - 住居費が手取りの30%を超えると警告
  - 「住居を10%削るほうが、サブスクを丸ごとゼロにするより効く」といった結論を自動で文章化
  - 節約の効果は運用利回りを含めた将来価値で計算するため、年数を伸ばすほど差が開くことが見える

## v1.1 (2026-09-06)
- **設定タブを追加。データの書き出し／読み込みに対応** — 別の端末へ移すには、書き出したファイルか文字列を相手の端末で読み込むだけ。データは端末の外に出ないまま移せる
- 保存されているデータの一覧と、全消去も設定から可能に
- **デスクトップ表示を改善** — 880px以上では横幅860pxに広がり、入力欄が2列に並ぶ

## v1.0 (2026-09-06)
- **タブにアイコン＋名前を表示**（アイコンだけでは意味が伝わらないため）。3群に整理
- **株 vs 不動産を追加** — 同じ自己資金を株に入れる場合と、レバレッジを効かせて不動産を買う場合で比較。税・空室・保有費・売却費用を考慮し、不動産が株に並ぶ価格上昇率を二分探索で算出
- **給油くらべを燃料4種に対応** — ガソリン／軽油／LPG／電気。電気はkm/kWh・kWh表記に自動で切り替わり、種別ごとに設定を保存
- **為替を3層表示に刷新** — 基準レート／銀行の現金レート／両替所レート（手入力）。主役は受取額ひとつだけ表示し、「銀行より+2.87%／基準より+1.06%」の1行で判断できる形に。詳細は折りたたみ
- **為替レートの自動更新を追加**（GitHub Actions、平日 09/12/15/18 KST）。Dunamuの公開APIから매매기준율・현찰 살 때・현찰 팔 때を取得し rates.json に保存
- 両替所は場所も重要なため、マイバンクの地図検索へのリンクを設置

## v0.9 (2026-09-06)
- **資産テーブルを追加** — 現金／投資／拘束／負債の4区分で棚卸しし、純資産と現金比率を自動計算。現金比率が10%を切ると警告
- **家賃 vs 保有を追加** — 월세・전세・매매を同じ手元資金から出発させて毎月シミュレーションし、N年後の純資産で比較
  - 住宅価格上昇率を二分探索し、「매매が賃貸に並ぶ分岐点は年◯%」を自動算出
  - 전세대출は利息のみ、住宅ローンは元利均等で計算。取得税・保有コスト・管理費・投資機会費用をすべて考慮
- 資産データは端末のlocalStorageのみに保存（リポジトリにも外部にも送信されない）

## v0.8 (2026-09-06)
- **ストップウォッチを追加**（ラップ計測、最速/最遅の色分け表示）
- **給油所くらべを追加** — 往復で余分に燃やす燃料代を上乗せして、遠くの安い店まで走る価値があるかを判定
- **現在価値を追加** — 将来の入金予定を記録として積み重ね、いまの価値に割り引いて合計する台帳型
- **為替を追加** — 銀行レート（ECB基準）を自動取得し、手入力した両替所レートと差額・％で比較。通貨ペアごとに保存
- タブを「基本ツール（タイマー・ストップウォッチ・カウンター）」と「計算ツール（給油所・現在価値・為替）」の2群に整理
- カウンター音を刷新：金属の共振と筐体の胴鳴りを加え、実物のタリーカウンターに近い打撃音に
- 10単位ごとの音を明確に強調（長さ9倍・ピーク音量3倍の3段構成「ガチャン」）
- 見出しに英字表記「Pace」を併記（日本語名は「間合い」のまま）

## v0.7 (2026-09-01)
- **アンビエントサウンドを追加**（設定でON、デフォルトOFF、音量調整可）
  - 研究知見に基づき、集中中はブラウンノイズ（集中力・ワーキングメモリ向上）、休憩中はピンクノイズ（リラックス・コルチゾール低下）を自動でクロスフェード再生
  - 外部音源不要、その場で合成
- スクリーンセーバーの太陽/月マーカーを廃止し、**山並みのシルエット風景**に変更。日照の明るさに応じて画面全体に明暗のオーバーレイをかける
- **日照モードON時、カード全体にも暖色フィルター**をかけるように（f.lux/Night Shiftと同じ原理でブルーライトを抑える、概日リズムに配慮した配色）

## v0.6 (2026-09-01)
- **日照モードを追加**（設定でON、デフォルトOFF）
  - 位置情報→タイムゾーン推定→PC時刻オフセット推定の3段階フォールバック、緯度経度は手動入力も可
  - 実際の日の出・日の入り時刻をもとに、背景が朝焼け／昼／夕焼け／夜と滑らかに変化
  - 「全体表示（スクリーンセーバー）」モード：画面いっぱいの空＋太陽/月マーカー＋大きな時計。タップで復帰
- カウンター音を再調整：通常音は機械的な「カチッ」、10単位ごとはリセットレバーの重い一撃音「カッチ」
- 再生／一時停止アイコンを角丸のSVGに刷新（アプリ全体の丸みのある言語と統一）
- 不具合修正：スクリーンセーバーの`hidden`属性がCSSに上書きされ常時表示されていた問題を修正

## v0.5 (2026-09-01)
- **不具合修正**: つまみ(ノブ)の位置がリングの弧の終点とズレていた計算ミスを修正
- **不具合修正**: 一時停止すると経過時間が消えて満タン表示に戻ってしまう不具合を修正。再開も一時停止した時点から続くように
- **不具合修正**: モバイルでビューポート設定が無く、画面全体が縮小されて見えていた問題を修正
- **反応速度改善**: タイマーのリング・カウンターの丸ボタンをpointerイベント直接反応に変更し、体感の遅延を解消
- ベルの音を集中→休憩／休憩→集中で区別（4種類とも音程・リズムが変化）
- 4サイクルごとに自動で長め休憩（通常の3倍）
- 今日の集中回数・合計時間を記録（日付が変わると自動リセット）
- ホーム画面に追加できるPWA対応（manifest.json・アイコン・Service Worker）
- カウンターに機械式カウンター風の「かちかち」音、10単位ごとに「しゃかっ」という違う音
- カウンター加算時にランダムなアイコンが浮かんで消える演出、対応端末では振動フィードバック
- リセットボタンに確認ポップアップ（進行中の時だけ確認、何もなければスルー）
- タップ時の青いハイライト表示を除去

## v0.4 (2026-09-01)
- **不具合修正**: タイマーとカウンターの両画面が同時に表示されてしまうバグを修正（`hidden`属性がCSSの`display:flex`に上書きされていた）

## v0.3 (2026-09-01)
- **カウンター機能を追加**: タイマーと切り替えられる新しいタブ
  - 円をタップで+1、キーボード操作対応（↑/Enter/Space=+1、↓/Backspace=−1）
  - 増減幅を設定可能
  - 値はブラウザに保存され、閉じても消えない

## v0.2 (2026-09-01)
- **操作方法を物理タイマー風に刷新**: 数字入力・ボタンから、リングをドラッグして時間を設定する方式へ
  - リング中央をタップで開始/一時停止（アイコンのみ、文字なし）
  - 設定（プリセット・休憩時間・ベル音）は歯車アイコンの奥に格納
- **不具合修正**: タブがバックグラウンドで間引かれても、正しい残り時間に復帰するよう内部の計測方式をタイムスタンプ基準に変更
- **不具合修正**: ダイヤルで時間を設定してもカウントダウンが反映されない不具合を修正

## v0.1 (2026-08-31)
- 初版公開
- 集中時間・休憩時間を設定できるタイマー
- ベル音を4種類から選択（鐘・チャイム・ビープ・木魚）、すべてWeb Audioでその場合成
- 集中↔休憩の自動切り替え
- 休憩中の行動案内メッセージ（画面から離れる・伸びをする等）をランダム表示
- プリセット（深い集中90/20・浅い集中25/5・52/17）
- MUJI×韓屋の方向性でデザイン（生成り色・木の色・控えめな線）
